TOKEN="${ADMIN_TOKEN:-<admin_token>}"
curl -k -X POST -H "Authorization: Bearer $TOKEN" -d '{"id":"2", "first_name":"new_name", "last_name":"new_2" }' https://localhost:4080/web-admin-api/update_user
